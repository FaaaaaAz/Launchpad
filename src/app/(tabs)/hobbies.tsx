import { ActivityDomainScreen } from '@/features/activities/components/ActivityDomainScreen';

/** Módulo de actividades. Toda la lógica vive en el componente compartido. */
export default function HobbiesScreen() {
  return <ActivityDomainScreen domain="hobby" />;
}
